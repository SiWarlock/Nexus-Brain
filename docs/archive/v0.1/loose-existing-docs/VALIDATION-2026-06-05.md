# project-brain — PRD Validation Report

> **Date:** 2026-06-05 · **Method:** multi-agent validation workflow (21 agents, ~1.04M tokens, 308 tool calls): 7 parallel finders (local probes + Context7 docs + web) → 13 adversarial independent re-checks → synthesis. Validates the rough-draft PRD dated 2026-06-03.
> **Provenance:** generated by the `prd-validation` workflow; RepoWise independently re-confirmed by the main session via PyPI + GitHub.

---

## 1. Verdict at a glance

| Verdict | Count | Notable |
|---|---|---|
| confirmed | 30 | Most CodeGraph schema, infra, RAG-stack, and competitor facts hold |
| changed | 4 | 33→35 project dirs; 21 MB→30 MB largest session; voyage landscape moved |
| refuted | A12 (channel), + un-served-intersection (skeptic) | see below |
| still-unverifiable | 3 | A13 bundling, M9 privacy-gate timing, the 39–53 ms/file probe |
| new-info | 5 | Voyage-4 family; **Amp** replaces consumer Cody; **Augment Code**; **Graphify**; **RepoWise**; **RepoAtlas** |

**Four headline conclusions:**

1. **A14 essentially flips to VERIFIED (cwd half) — with a correction.** Both finder and an independent skeptic confirmed on 62 real Codex rollout files that `session_meta.payload.cwd` is present on the first record of every file (62/62). **The "+model" half is WRONG** — model lives in `turn_context.payload.model`, which is **absent in 51/62 files**, so model is not reliably per-file recoverable. Also demote "session_meta = 1 per file" (5 multi-turn files had 2–73). `session_meta.payload.git = {branch, commit_hash, repository_url}` gives branch/commit directly.

2. **The moat thesis is the real risk — RepoWise.** The "we are the only…" un-served-intersection claim was **refuted by an independent skeptic** and re-confirmed by the main session: **RepoWise** (PyPI `repowise` v0.16.0, 2026-06-03, AGPL-3.0; self-hosted/no-telemetry; multi-repo "federated MCP"; tree-sitter graph + per-commit LLM wiki; MCP for any frontier agent; **LanceDB** substrate) occupies **all four axes**. The literal "only" claim is false as of 2026-06. **Owner positioning call required.**

3. **A12 was a load-bearing claim that's WRONG (inverted).** CodeGraph **IS** an npm package (`@colbymchenry/codegraph` 0.9.9, MIT) — npm-primary + GitHub-Releases fallback (esbuild thin-shim + per-platform optionalDependency bundling its own Node-24 runtime). **NOT** brew/cargo/pipx. Flip A12 UNVERIFIED→VERIFIED with inverted content. (Finder + skeptic agree.)

4. **No other [VERIFIED] claim was falsified.** schema_versions v4 (corroborated on 3 live DBs), one-index-per-repo, WAL concurrency, no `watch`/`daemon` subcommand, Citations API, 1M context (GA Mar-2026, flat pricing), prompt-cache pricing, and the entire competitor table all held.

**Disagreements flagged:** (a) un-served intersection — finder "confirmed-with-erosion" vs skeptic "refuted (RepoWise)"; skeptic is stronger (named, sourced counterexample). (b) 300s idle-exit — finder "still-unverifiable (prior probe)" vs skeptic "source-confirmed" (`DEFAULT_IDLE_TIMEOUT_MS = 300_000` hard-coded in the shipped daemon binary; "~1s lag" in server-instructions). Adopt the skeptic's stronger sourcing; only the 39–53 ms/file figure remains single-probe.

---

## 2. Marker changes

| Claim | Current | → Recommended | Why | Conf. | Adv. agree? |
|---|---|---|---|---|---|
| **A6 / FR-4.6** prompt-cache pricing + 1h→5m TTL regression | ASSUMPTION | **VERIFIED** | Docs confirm 1.25×/2×/0.1×; regression = issue #46829 (early-Mar-2026); mitigation `ENABLE_PROMPT_CACHING_1H=1` | high | — |
| **A12** CodeGraph install channel | UNVERIFIED | **VERIFIED (content inverted)** | It IS npm; NOT brew/cargo/pipx | high | yes |
| **A14** session_meta cwd (+model) | UNVERIFIED | **VERIFIED (correct: cwd yes; model NO)** | cwd at `payload.cwd` (62/62); model is in `turn_context`, absent 51/62 | high | yes |
| **A13** skills-bundling | OPEN | **OPEN (keep)** | Product decision; precondition (vendorable @ b2c8846) verified | high | yes |
| 21 MB largest session | ASSUMPTION | **VERIFIED (~30 MB)** | Actual max 30.21 MiB; several >20 MiB | high | yes |
| 33 Claude project dirs | VERIFIED | **VERIFIED (~35, illustrative)** | Now 35 dirs (31 with transcripts); drifts upward | high | yes |
| 300s idle-exit + ~1s lag | VERIFIED | **VERIFIED (source-confirmed)** | Hard-coded constant in shipped binary; ~1s lag in server-instructions | high | yes (skeptic upgrades) |
| 39–53 ms/file | VERIFIED | **VERIFIED (annotate: single-probe)** | Not re-measurable without a timed live-daemon load test | med | — |
| FastMCP 3.0 date | none | **VERIFIED, fix date** | GA **Feb 18, 2026** (not "Jan 2026"); pin ≥3.2 | high | — |
| voyage-code-3 facts | none | **VERIFIED + Voyage-4 note** | Numbers exact; Voyage-4 family (Jan-2026, MoE) now exists — benchmark first | high | yes |
| bge-m3 / qwen3 / nomic-v2 | none | **VERIFIED** | Current; nomic-v2 = `nomic-embed-text-v2-moe` | high | — |
| LanceDB native FTS/hybrid | VERIFIED | **VERIFIED (keep)** | optimize()/flat-scan caveat exact; Tantivy dropped | high | — |
| CodeHierarchy / cAST / Contextual / Late-Chunking | none | **VERIFIED** | All exact; note 67% = contextual **+ reranking** | high | — |
| Rerankers (rerank-2.5 / bge-v2-m3) | none | **VERIFIED** | Current; add rerank-2.5-lite + qwen3-reranker | high | — |
| Citations API | VERIFIED | **VERIFIED (keep)** | GA; 10%→0% is single-customer (Endex) | high | — |
| FR-4.4 long-context / 1M | VERIFIED | **VERIFIED (keep)** | Anthropic's exact "<200K → skip RAG"; 1M GA Mar-2026 | high | — |
| Infra (Watchman/WAL/launchd/systemd) | VERIFIED | **VERIFIED (1 precision)** | launchd `TimeOut` "no longer implemented" — strengthens "worker MUST self-terminate" | high | — |
| schema_versions v4 | VERIFIED | **VERIFIED (keep)** | 3 live DBs; `unresolved_refs` richer (reference_name/kind/candidates/file_path) | high | — |
| no watch/daemon subcommand | VERIFIED | **VERIFIED (reconcile §13)** | §13 line 607 wrongly marks [UNVERIFIED]; add `references` edge kind | high | — |
| commit b2c8846 / layer-docs incremental | VERIFIED | **VERIFIED (1 fix)** | "ahead of origin" stale → "AT origin/main (pushed)" | high | yes |
| content.json / manifest fields / contract files | VERIFIED | **VERIFIED (keep)** | All present; manifest verified vs schema, not a live instance | high | — |
| **Un-served intersection** | none (implied "only") | **DOWNGRADE — narrow & name neighbors** | RepoWise hits all 4 axes; Augment/Graphify partially erode | high | NO — direct disagreement |
| Cody consumer shutdown / **Amp** | none | **add Amp** | Cody Free/Pro retired Jul-2025; Amp is the successor | high | yes |
| **Augment Code** (new) | absent | **add VERIFIED row** | Multi-repo + docs+code + frontier-MCP, cloud-only (3/4 axes) | high | yes |
| **Graphify** (new) | absent | **add VERIFIED row (corrected)** | Local docs+code graph for a frontier agent; HAS Global Graph + `--watch` + post-commit hook; gap = static snapshots, no curated docs pipeline | high | yes |
| **RepoWise** (new) | absent | **add row — the all-4-axes neighbor** | confirmed real & decisive | high | (skeptic + main) |
| RepoAtlas (new) | absent | **optional mention** | Local + portfolio only (static scanner) | high | yes |

---

## 3. Recommended PRD edits (HIGH-impact first)

**A-row assumptions:** A12 rewrite (npm-primary + GH-Releases fallback); A14 flip-with-correction (cwd yes, model no, demote "1 per file" + thread-name fallback); A6 ASSUMPTION→VERIFIED (+ `ENABLE_PROMPT_CACHING_1H=1`).

**§9.7 + FR-13:** rely on the *first* record (always session_meta+cwd) not "record[0]" generally (a Claude head summary record may lack cwd; a Codex file's first record is reliably session_meta+cwd); cite `turn_context.payload.model` as the best-effort/often-absent model source; `session_meta.payload.git` for branch/commit; "21 MB"→"~30 MB"; "33 dirs"→"~35".

**Appendix B + RESEARCH §6:** add Augment Code, Sourcegraph Amp (+ Cody consumer-tier shutdown), Graphify (corrected), RepoWise; optional RepoAtlas + Bloop "BloopAI shut down Apr-2026"; soften Cody "300k+" to "100k+".

**Moat statement (Appendix B line 680, RESEARCH §6 line 121, §1 TL;DR, §5):** retire the literal "we are the only…"; narrow to the genuinely-defensible legs (live per-repo federation + local-first portfolio scale + authored/anchored curated-docs pipeline + Session-Memory provenance) and name the nearest neighbors. **Owner decides the reframing (see §6).**

**RESEARCH §5/§9.5:** FastMCP "Jan 2026"→"GA Feb 18, 2026", pin ≥3.2; Voyage-4 benchmark note; Contextual-Retrieval 67% = +reranking; re-date Citations URL; 1M GA flat pricing; launchd `TimeOut` "no longer implemented".

**RESEARCH §2 / FR-5.5:** add `references` edge kind; note `unresolved_refs` carries reference_name/reference_kind/candidates/file_path/language (richer seam).

---

## 4. Internal-consistency fixes (ranked)

**HIGH**
1. **Two conflicting root dirs** — FR-10.2(d) `~/.brain/registry.json` vs FR-3.1/§9.1 `~/.project-brain/`. Pick one (recommend `~/.project-brain/` for everything); state the full layout once.
2. **Python stack vs npm/npx install** — whole stack is Python (FastMCP/LlamaIndex/LanceDB/Ollama) but install is `npm i -g`. Add an FR stating the install topology (e.g. pipx/uv Python package; npx as a bootstrap shim that provisions a pinned venv) and have FR-10.2 detect→install→verify→degrade the Python runtime like it does CodeGraph.

**MEDIUM**
3. **Broken cross-refs** — `§4.3/4.4/4.5` cite the *personas* section; the rules live in **FR-4.3/4.4/4.5**. Global fix.
4. **Trust-boundary contradiction** — FR-4.1 says stdio MCP is the ONLY code-touching component, but the web UI (FR-7) needs HTTP/socket. State the server exposes stdio + localhost HTTP through the *same* DLP/policy gate; add UI transport to §6 scope.
5. **Marker clash** — "no watch/daemon subcommand" is [VERIFIED] in A3/FR-2.7 but [UNVERIFIED] in §13 line 607. Reconcile to [VERIFIED].
6. **Team mode in-scope vs not-shippable** — label W2/W7 "CONDITIONAL — blocked on A8 key-dist/revocation" in §14 *and* inline in §8 scores.

**LOW**
7. §9.1 prose store list (line 404) omits `doc_sources/sessions/episodes/commit_links` — complete it.
8. FR-7.2 "plain⇄deep = no LLM cost" stated unconditionally vs FR-4.7/M4 scoping to dual-register cc-crew chunks — scope FR-7.2 to match.

**Roadmap / scoring**
- M9 F=4→F=3 (the FR-13.7 privacy gate is the real cost) — or split M9a (Claude read, v1) / M9b (privacy gate, v1, explicit dependency).
- V10 staging: pin full OWNED auto-refresh to v1.1 (drop "v1.1→v2"); add external-dependency risk (cc-crew incremental layer-docs ship date).
- v0 ships M4/M5 but has no ingest → scope to degenerate forms (inline dual-register only; stamp = HEAD-sha+model only).
- W13 Code-Only Brain mis-staged in v2 — M8 + the v1 stack already deliver it; recharacterize as a v1 scope-definition (the A7 call).
- MVP "F 4–5, none infra-blocking" optimistic — M7 (A12/A13/Python-on-npm) and M9 (privacy gate) are F3 real-effort.
- "MVP" vs "v1" vocabulary collision — state MVP spans v0+v1; use §14 phase names in every "Ships in" column.

---

## 5. Candidate NEW features (ranked; incl. the owner's 3)

| # | Feature | Rationale | Tier | Coverage | Key risk |
|---|---|---|---|---|---|
| 1 | **Auto-sync commit-hook (owner #1)** | re-index cheaply on commit; manual `brain sync` fallback | v1 (w/ M7) | **already-covered** (FR-2.2/10.4e + FM-1) | "always-fresh" applies to the index, NOT the idle CODE GRAPH (daemon idle-exits 300s) |
| 2 | **Make the hook ASYNC/non-blocking** | the "fast, not slow" half — FR-2.2 never says fire-and-forget | v1 | **partial-gap** | race a follow-up commit → coalesce via FR-2.8 writer lock |
| 3 | **Provider-routing policy enforcement** | gate ALL providers (OpenRouter/Codex-sub) through policy.yaml allowlist + ZDR attestation | v2 (gates #5) | **partial-gap** | one mis-route leaks a local-only repo → sev-1 |
| 4 | **Per-project policy lifecycle + safe default** | `brain add` MUST create policy.yaml defaulting to local-only | v1 | **gap** | missing-policy repo silently defaulting to cloud = sev-1 |
| 5 | **Desktop Copilot Chat + model-switching (owner #2)** | Mac/Win app, multi-provider picker, inline citations, drift/déjà-vu panes | v2 (revise §3 first) | **new + conflicts** | multi-provider (esp. OpenRouter fan-out) breaks single-trust-boundary unless gated (#3); reverses a §3 non-goal |
| 6 | **Teams & Collaboration (owner #3)** | signed `.brain` bundles + Brain Hub + per-project key ACL | v2 stretch (after A8) | **partial-gap** (W2/W7/W8; blocked on A8) | revocation in offline model can only gate FUTURE deltas |
| 7 | **Credential management** | `brain setup` detects/validates cloud keys, OS keychain | v1 | **gap** | first `brain add` w/ no key = undefined today |
| 8 | **Grounded-Answer Eval Harness** | CI-gate the grounding gate's strip/refuse precision (maps to owner's "brain self-evaluation") | v1 (w/ V3) | **new** | maintaining a golden Q/A+citation set |
| 9 | **Anchor Auto-Repair suggestions** | propose the new file:line when an anchor goes stale (graph-confident, never auto-apply to FOREIGN) | v1.1 | **new** | a wrong suggestion = false confidence; label "suggested, unverified" |
| 10 | **Cost/Budget Governor** | per-day/per-project ceilings on hydration tokens/embeds/reranks | v1.1 | **new** | aggressive cap silently degrades answers → surface in stamp |
| 11 | **RBAC/identity model (FR-6.6)** | for v1 local-single-user roles are a no-op; defer real RBAC to team-mode w/ authed HTTP | v1 (clarify) | **gap** | roles referenced as enforceable but unspecified |
| 12 | **`brain remove <repo>` (FR-10.7)** | reverse every per-repo mutation `brain add` made | v1 | **gap** | clobbering a husky/lefthook chain |
| 13 | **Operational health surface (FR-9.2)** | queryable health log via `brain status`/`brain doctor` | v1 | **gap** | — |
| 14 | **Single-user multi-machine portability (FR-3.5)** | export/import registry + policies; derived stores rebuild locally | v1.1 | **gap** | users assuming cloud-sync |
| 15 | **Brain store schema migration (FR-3.6)** | detect mismatch, refuse-to-corrupt, blue-green rebuild | v1.1 | **gap** | — |
| 16 | **Multi-client read concurrency clause** | N MCP servers safely read one brain store (read-only WAL + LanceDB multi-reader) | v1 | **gap** | — |

**Note on the owner's 3:** #1 is largely *already in the PRD* (only the async latency contract is missing) — the honest caveat is the code-graph-freshness gap. #2 and #3 are heavy: both break the privacy model unless provider-routing enforcement lands first, and #2 also reverses an explicit §3 non-goal. Build #2/#6 as thin shells over the *same* MCP server so they never become a second trust boundary.

---

## 6. Still-unverifiable / needs owner decision

1. **RepoWise — how far to retreat the moat? (HIGHEST).** Confirmed all-4-axes (PyPI/GitHub/MarkTechPost). Owner must decide how to reframe "we are the only…" — narrow to the defensible legs and name neighbors (recommended), or reposition around Session Memory / curated-authored-docs fusion.
2. **A13 skills-bundling (OPEN).** Product decision (a/b/c); default vendor a pinned copy (b). Precondition verified.
3. **39–53 ms/file timing.** Single-probe; not re-measurable without a timed live-daemon load test. (300s idle-exit + ~1s lag ARE now source-confirmed.)
4. **M9 privacy-gate timing & FR-13.7 redaction scope** — effort/risk judgment.
5. **Python-on-npm packaging topology** — deliberate engineering decision (pipx/uv vs Node shim).
6. **Desktop app + multi-provider routing (#2) and team revocation model (#3)** — §3 non-goal revision + A8 key-management spec.

---

*Sources for the RepoWise re-confirmation: [PyPI repowise](https://pypi.org/project/repowise/), [github.com/repowise-dev/repowise](https://github.com/repowise-dev/repowise), [MarkTechPost tutorial (2026-05-15)](https://www.marktechpost.com/2026/05/15/how-to-build-repository-level-code-intelligence-with-repowise-using-graph-analysis-dead-code-detection-decisions-and-ai-context/).*
